#!/bin/bash

mkdir -p ~/.config/jitas-chromium

chromium --class=jitas --tag=jitas --silent-launch --incognito --mute-audio \
    --user-data-dir=~/.config/jitas-chromium

systemctl --user stop video.service
